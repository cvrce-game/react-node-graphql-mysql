# React GraphQL Client

- Uses Apollo Client to fetch data from Node GraphQL server
- Displays users from MySQL database

## Setup
1. Install dependencies: `npm install`
2. Start app: `npm start`

Make sure the Node server is running at `http://localhost:4000/graphql`.